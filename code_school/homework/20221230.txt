1. Make a presentation on lambda (that explains what it does)
2. Use lambda to make another variation of order_str.py 
(Lambda function that you can apply to a list of strings to get the longest element)